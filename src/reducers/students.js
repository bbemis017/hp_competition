  
/**
 * Starter Reducer
 * I'm just creating this to get rid of the error associated with not have any reducers
 * Once we have real reducers this should be removed
 */

const INITIAL_STATE = {};

export default function (state = INITIAL_STATE, action) {
   switch(action.type){
       default:
           return state
   }
}